#avaScript is the language of the browser.
# It runs directly in the page, responds to user actions, and updates content without reloading.
# This is what makes the web interactive.

# What is Javascript?

#JavaScript is a programming language that runs inside a web browser.
# Unlike Python, which runs on a server, JavaScript runs on the user's device.
# It can read and change anything on a web page, respond to clicks and keyboard events, and make requests to external APIs.

#Python and JavaScript solve similar problems with different syntax.
# The core concepts (variables, loops, functions, conditionals) are the same.
# The differences are mostly in how you write them.

# Python
# name = "Brian"
# age = 28
# print(f"Name: {name}, Age: {age}")

# JavaScript
# const name = "Brian";
# const age = 28;
# console.log(`Name: ${name}, Age: ${age}`);

# Variables: let and const

# const declares a variable that will not be reassigned.
# let declares a variable that may be reassigned later. Use const by default.
# Switch to let only when you know the value will change.

# Keyword	 Reassignable?	    Use case
# const	        No	           Configuration, names, fixed data
# let	        Yes	           Counters, running totals, loop variables
# var	        Yes	           Old code only. Avoid in new code.

#Tip: In Python, variable type is determined at runtime and can change freely.
# In JavaScript, const and let do not restrict the type.
# They only restrict whether the variable name can be pointed at a new value.
# A const array can still have items added to it.


